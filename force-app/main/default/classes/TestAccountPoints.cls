@isTest
private class TestAccountPoints {
    @isTest static void TestOrderInsertedAddsPointsToAccount() {
        // Create mock insert of order that has points
        
        // Start test
        
        // Query the record we want and check the field data
        
        // Assert that field amount equals what we think it should
        
    }
}